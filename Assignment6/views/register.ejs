<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Register</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
  <%- include("partials/navbar", { page: "/register" }) %>

  <div class="hero min-h-screen bg-base-200">
    <div class="hero-content flex-col max-w-md w-full">

      <% if (successMessage) { %>
        <div class="alert alert-success mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
          </svg>
          <span><%= successMessage %></span>
        </div>
        <a href="/login" class="btn btn-success">Proceed to Log in</a>

      <% } else { %>
        <form method="POST" action="/register" class="card bg-base-100 p-8 shadow-xl w-full">
          <h1 class="text-3xl font-bold mb-4">Register</h1>

          <input type="text" name="userName" placeholder="User Name" value="<%= userName %>" required class="input input-bordered mb-4" />
          <input type="email" name="email" placeholder="Email Address" required class="input input-bordered mb-4" />
          <input type="password" name="password" placeholder="Password" required class="input input-bordered mb-4" />
          <input type="password" name="password2" placeholder="Confirm Password" required class="input input-bordered mb-4" />
          <button class="btn btn-primary">Register</button>

          <% if (errorMessage) { %>
            <div class="alert alert-error mt-4">
              <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span><%= errorMessage %></span>
            </div>
          <% } %>
        </form>
      <% } %>

    </div>
  </div>
</body>
</html>
